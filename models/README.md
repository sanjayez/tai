# Models

Place local runtime model files here during development.

Do not commit large model artifacts such as `.gguf`, `.onnx`, or `.bin` files.

Expected future layout:

```text
models/
  llm/
    model.gguf
  ocr/
    ...
```

